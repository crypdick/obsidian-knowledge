"""Primer builder — produces a context string from top-ranked vault hits.

Stub — implementation deferred to a later task.
"""
from __future__ import annotations

from lib.vault_index.indexer import Hit


def build_primer(hits: list[Hit]) -> str:
    """Return a formatted context string from the ranked hits."""
    raise NotImplementedError
